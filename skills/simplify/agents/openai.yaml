interface:
  display_name: "Simplify"
  short_description: "Simplify code without changing behavior"
  default_prompt: "Use $simplify to simplify the code we just changed without altering its exact behavior."
